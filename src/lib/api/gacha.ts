export default class GachaApi {
	// TODO: Discuss about this, whether to send ONLY the auth_key and game_biz and let backend do all the fetch, or send fetch one by one
	fetchFromGenshin(_start: number, _count: number, _authKey: string) {}
}
